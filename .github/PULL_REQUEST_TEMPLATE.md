<!-- PR title = Conventional Commit + issue ref, e.g. feat(auth): add lockout window (#123) -->

## Summary

<!-- What changed and why. "See title" is not a description. -->

## Test plan

<!-- How you verified it and WHERE. Link the evidence: CI run, test name, screenshot, recording.
     Local-only is acceptable pre-merge; it does not close the item. -->

- [ ] `check:format` / `check:lint` / `check:types` / `check:test` green before review was requested

## Dependencies

<!-- One per line, or "None". Keep downstream PRs in draft until these merge. -->

Depends on #

## Risk & rollback

<!-- DELETE this section unless the PR touches: migrations/schema, auth or identity, payments,
     running-number allocation, or infra. Otherwise state the deploy-window sequence, what the
     old code still running sees, and how to undo. -->
